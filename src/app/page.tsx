// This component should handle redirection.
// For Next.js App Router, redirection is often handled in middleware or a server component.
// Here's a simple client-side redirect for demonstration.
// A server-side redirect would be more appropriate for production.

export default function HomePage() {
  return null;
}